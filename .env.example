# ==============================================================================
# LINKEDIN SCRAPER CONFIGURATION
# ==============================================================================
# Optional: Only needed for headless scraping if session cookies expire.
# For normal GUI usage, you log in manually once and it retains the session.
LINKEDIN_EMAIL=your-email@example.com
LINKEDIN_PASSWORD=your-password

# Scraper mode and source settings
SCRAPER_MODE=search
CONNECTIONS_CSV=Connections.csv
OUTPUT_CSV=UNT_Alumni_Data.csv
SEARCH_DISCIPLINES=
SCRAPE_RESUME_MAX_AGE_DAYS=7
UPDATE_FREQUENCY=6 months

# Browser/session behavior
HEADLESS=false
USE_COOKIES=true
LINKEDIN_COOKIES_PATH=linkedin_cookies.json

# Scrape timing behavior
MIN_DELAY=60
MAX_DELAY=240
PAGE_SETTLE_SECONDS=0
POST_SECTION_WAIT_SECONDS=0
EDU_READY_TIMEOUT_SECONDS=30

# Optional GUI stop limits (0 means infinite)
GUI_MAX_PROFILES=0
GUI_MAX_RUNTIME_MINUTES=0

# Review flagging criteria
FLAG_MISSING_EXPERIENCE_DATA=true

# ==============================================================================
# LINKEDIN OAUTH (WEBSITE LOGIN)
# ==============================================================================
# Required for user authentication on the web dashboard.
# Get these by creating an app in the LinkedIn Developer Portal.
LINKEDIN_CLIENT_ID=your-client-id
LINKEDIN_CLIENT_SECRET=your-client-secret
LINKEDIN_REDIRECT_URI=http://127.0.0.1:5000/auth/linkedin/callback

# ==============================================================================
# FLASK BACKEND CONFIGURATION
# ==============================================================================
# Cryptographic key used for securely signing session cookies.
SECRET_KEY=your-flask-secret-key

# ==============================================================================
# MYSQL DATABASE CONNECTION
# ==============================================================================
# Credentials for the cloud/local MySQL database storing alumni profiles.
# Scraper logic will fall back to local SQLite if this is unavailable.
MYSQLHOST=your-mysql-host
MYSQLUSER=your-mysql-user
MYSQLPASSWORD=your-mysql-password
MYSQL_DATABASE=your-database-name
MYSQLPORT=37157

# Fallback and local/offline behavior
USE_SQLITE_FALLBACK=1
DISABLE_DB=0

# Optional backend startup controls
QUIET_HTTP_LOGS=1
SEED_ON_STARTUP=0

# ==============================================================================
# ARTIFICIAL INTELLIGENCE ENGINES
# ==============================================================================
# Required for running relevance scoring, degree/major normalization, and parsing.
GROQ_API_KEY=your-groq-key
USE_GROQ=true
